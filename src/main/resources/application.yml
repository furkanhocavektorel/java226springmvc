server:
  port: 8081
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:5432/java226
  jpa:
    hibarnate:
      ddl-auto: update
      show-sql: true
      format-sql: true